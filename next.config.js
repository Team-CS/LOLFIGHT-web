/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "placehold.co",
      "lolfight.kr",
      "static.lolesports.com",
    ],
  },
};

module.exports = nextConfig;
